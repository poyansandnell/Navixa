kind = "mobile"
previewPath = "/"
title = "Navixa"
version = "1.0.0"
id = "artifacts/fleet-arena"
router = "expo-domain"

[[services]]
name = "expo"
ensurePreviewReachable = "/status"
paths = [ "/" ]
localPort = 19262

[services.development]
run = "pnpm --filter @workspace/navixa run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/navixa", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/navixa", "run", "serve" ]

[services.env]
PORT = "19262"
BASE_PATH = "/"
